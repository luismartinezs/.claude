---
description: "The project has a knowledge base, a topic map, a niche GEO strategy, a build-validated blog, and a…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **GEO Pipeline (Bootstrap)** workflow playbook (`bootstrap-geo-content`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/workflows/**/bootstrap-geo-content.md` in the current project
2. `~/.claude/playbooks/workflows/**/bootstrap-geo-content.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
