import { APP_CONFIG } from '@repo/config';

export const legalConfig = {
  websiteTitle: APP_CONFIG.name,
  websiteUrl: APP_CONFIG.url,
  companyName: APP_CONFIG.companyName,
  emailContact: APP_CONFIG.email,
  address: '123 Main St, Your City, ST 00000 USA',
};
