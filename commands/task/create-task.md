---
description: "Design and generate a comprehensive, executable Task Definition file (Markdown) based on a user's rough…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Create New Task** task playbook (`create-task`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/create-task.md` in the current project
2. `~/.claude/playbooks/tasks/**/create-task.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
