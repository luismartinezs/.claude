---
description: "Identify the root cause of a bug through evidence-based investigation, producing a diagnosis with a verified…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Diagnose Bug** task playbook (`diagnose-bug`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/diagnose-bug.md` in the current project
2. `~/.claude/playbooks/tasks/**/diagnose-bug.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
