---
description: "Given a goal (input + desired output), discover available tools, inventory existing tasks, and compose a…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Compose Workflow** task playbook (`compose-workflow`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/compose-workflow.md` in the current project
2. `~/.claude/playbooks/tasks/**/compose-workflow.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
