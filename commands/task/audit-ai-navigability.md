---
description: "Assess whether the codebase is optimized for AI-assisted development (Claude Code specifically), scoring…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Audit AI Navigability** task playbook (`audit-ai-navigability`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/audit-ai-navigability.md` in the current project
2. `~/.claude/playbooks/tasks/**/audit-ai-navigability.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
