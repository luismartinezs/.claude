---
description: "`docs/spec.md`, `docs/plan.md`, `docs/milestones/M0.md`..`Mn.md` and `docs/runbook.md` exist. The operator…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Plan Buildout** workflow playbook (`plan-buildout`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/workflows/**/plan-buildout.md` in the current project
2. `~/.claude/playbooks/workflows/**/plan-buildout.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
