---
description: "Configure production deploy infrastructure for a new project on an existing multi-app VPS, producing PM2…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Setup Deploy Infrastructure** task playbook (`setup-deploy`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/setup-deploy.md` in the current project
2. `~/.claude/playbooks/tasks/**/setup-deploy.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
