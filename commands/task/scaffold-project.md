---
description: "Bootstrap a complete monorepo from a product spec, producing a working skeleton that follows the project's…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Scaffold Project** task playbook (`scaffold-project`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/scaffold-project.md` in the current project
2. `~/.claude/playbooks/tasks/**/scaffold-project.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
