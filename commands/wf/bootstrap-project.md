---
description: "A complete monorepo skeleton is scaffolded (Bun workspaces, Astro + Vue, Hono, Drizzle, Tailwind) as vertical…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Bootstrap Project** workflow playbook (`bootstrap-project`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/workflows/**/bootstrap-project.md` in the current project
2. `~/.claude/playbooks/workflows/**/bootstrap-project.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
