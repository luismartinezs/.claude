---
description: "Convert a business brief into a prioritized `research-plan.md`: a list of scoped research topics that, once…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Plan Knowledge Research** task playbook (`plan-research`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/plan-research.md` in the current project
2. `~/.claude/playbooks/tasks/**/plan-research.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
