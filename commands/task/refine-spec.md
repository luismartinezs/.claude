---
description: "Transform a rough, unreviewed spec draft into a clear, actionable specification by conducting a structured…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Refine Spec** task playbook (`refine-spec`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/refine-spec.md` in the current project
2. `~/.claude/playbooks/tasks/**/refine-spec.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
