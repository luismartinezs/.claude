---
description: "Generate a complete CSS design system (custom properties file) from 3 seed inputs, producing a coherent…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Create Design System** task playbook (`create-design-system`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/create-design-system.md` in the current project
2. `~/.claude/playbooks/tasks/**/create-design-system.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
