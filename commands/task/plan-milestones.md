---
description: "Convert a refined spec into `docs/plan.md`: a sequence of milestones ordered by what can be verified without…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Plan Milestones** task playbook (`plan-milestones`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/plan-milestones.md` in the current project
2. `~/.claude/playbooks/tasks/**/plan-milestones.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
