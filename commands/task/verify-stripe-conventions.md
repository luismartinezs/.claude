---
description: "Converge this app's dashboard-created Stripe Product and Price to the attribution convention (metadata `{app,…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Verify Stripe Conventions** task playbook (`verify-stripe-conventions`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/verify-stripe-conventions.md` in the current project
2. `~/.claude/playbooks/tasks/**/verify-stripe-conventions.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
