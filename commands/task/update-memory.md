---
description: "Extract high-value architectural decisions, patterns, or \"lessons learned\" and persist them into…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Update Memory** task playbook (`update-memory`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/update-memory.md` in the current project
2. `~/.claude/playbooks/tasks/**/update-memory.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
