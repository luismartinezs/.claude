---
description: "Make the site legible and reachable to AI crawlers: verify crawler access, emit `llms.txt`, wire FAQPage…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Set Up Distribution Surface** task playbook (`setup-distribution-surface`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/setup-distribution-surface.md` in the current project
2. `~/.claude/playbooks/tasks/**/setup-distribution-surface.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
