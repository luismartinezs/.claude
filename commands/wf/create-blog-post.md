---
description: "A researched, drafted, audited, fact-checked article sits in the content collection with `draft: true`,…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Create Blog Post** workflow playbook (`create-blog-post`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/workflows/**/create-blog-post.md` in the current project
2. `~/.claude/playbooks/workflows/**/create-blog-post.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
