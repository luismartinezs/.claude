---
description: "Diagnose what is actually wrong with an existing UI, separate defects from taste, and produce a ranked change…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Critique UI** task playbook (`critique-ui`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/critique-ui.md` in the current project
2. `~/.claude/playbooks/tasks/**/critique-ui.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
