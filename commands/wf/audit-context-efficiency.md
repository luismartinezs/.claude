---
description: "Codebase is assessed against AI navigability criteria, hotspots are identified, and the most impactful fixes…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Audit Context Efficiency** workflow playbook (`audit-context-efficiency`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/workflows/**/audit-context-efficiency.md` in the current project
2. `~/.claude/playbooks/workflows/**/audit-context-efficiency.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
