---
description: "Conduct a rigorous, adversarial code review that produces a scored assessment with specific, actionable…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Review Code** task playbook (`review-code`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/review-code.md` in the current project
2. `~/.claude/playbooks/tasks/**/review-code.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
