---
description: "Measure the contrast of every text and meaningful graphic in a UI **as it actually renders**, fix what fails,…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Audit Colour Contrast** task playbook (`audit-contrast`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/audit-contrast.md` in the current project
2. `~/.claude/playbooks/tasks/**/audit-contrast.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
