---
description: "Convert an audited, fact-checked draft into a production-ready file in the project's content collection, with…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Publish Blog Post** task playbook (`publish-blog-post`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/publish-blog-post.md` in the current project
2. `~/.claude/playbooks/tasks/**/publish-blog-post.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
