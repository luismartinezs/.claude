---
description: "Extract a feature from a reference codebase into a reusable `port-<feature>` Claude Code skill that any…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Create Port Skill** task playbook (`create-port-skill`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/create-port-skill.md` in the current project
2. `~/.claude/playbooks/tasks/**/create-port-skill.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
