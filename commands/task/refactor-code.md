---
description: "Improve the internal structure of existing code without changing its external behavior, scoped to the…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Refactor Code** task playbook (`refactor-code`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/refactor-code.md` in the current project
2. `~/.claude/playbooks/tasks/**/refactor-code.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
