---
description: "Conduct an adversarial security review of the codebase, checking for OWASP Top 10 vulnerabilities,…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Security Audit** task playbook (`audit-security`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/audit-security.md` in the current project
2. `~/.claude/playbooks/tasks/**/audit-security.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
