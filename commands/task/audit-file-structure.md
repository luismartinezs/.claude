---
description: "Assess whether existing vertical slices have outgrown their single file, producing a prioritized split plan…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Audit File Structure** task playbook (`audit-file-structure`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/audit-file-structure.md` in the current project
2. `~/.claude/playbooks/tasks/**/audit-file-structure.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
