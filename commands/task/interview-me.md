---
description: "Run one short interview (5 minutes, hard cap) that extracts durable personal context from Luis and folds it…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Interview Me** task playbook (`interview-me`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/interview-me.md` in the current project
2. `~/.claude/playbooks/tasks/**/interview-me.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
