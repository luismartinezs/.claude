---
description: "Produce or close out one file at `docs/milestones/M{n}.md`: the self-contained brief a fresh session reads…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Write Milestone Card** task playbook (`write-milestone-card`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/write-milestone-card.md` in the current project
2. `~/.claude/playbooks/tasks/**/write-milestone-card.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
