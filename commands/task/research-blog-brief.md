---
description: "Produce a structured research brief for a single GEO-optimized blog post, containing competitive…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Research Blog Brief** task playbook (`research-blog-brief`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/research-blog-brief.md` in the current project
2. `~/.claude/playbooks/tasks/**/research-blog-brief.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
