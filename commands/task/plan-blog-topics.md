---
description: "Turn the business brief and knowledge base into `blog-topics.md`: a prioritized map of topic clusters and…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Plan Blog Topics** task playbook (`plan-blog-topics`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/plan-blog-topics.md` in the current project
2. `~/.claude/playbooks/tasks/**/plan-blog-topics.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
