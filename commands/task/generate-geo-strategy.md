---
description: "Produce `geo-strategy.md`: the niche-specific application layer that tells every downstream writing task…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Generate GEO Strategy** task playbook (`generate-geo-strategy`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/generate-geo-strategy.md` in the current project
2. `~/.claude/playbooks/tasks/**/generate-geo-strategy.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
