---
description: "UI is designed, implemented, visually verified via screenshots, responsive, and reviewed. Ready to merge."
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Build UI** workflow playbook (`build-ui`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/workflows/**/build-ui.md` in the current project
2. `~/.claude/playbooks/workflows/**/build-ui.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
