---
description: "Stand up a static, build-validated blog in the project's Astro app, and emit…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Set Up Blog Infrastructure** task playbook (`setup-blog-infra`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/setup-blog-infra.md` in the current project
2. `~/.claude/playbooks/tasks/**/setup-blog-infra.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
