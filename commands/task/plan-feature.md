---
description: "Create a comprehensive, step-by-step implementation plan for a new feature, ensuring no architectural…"
argument-hint: [input]
---
<!-- generated by generate-command-stubs.ts — do not edit by hand -->

Run the **Plan Feature Implementation** task playbook (`plan-feature`).

Resolve the file per CLAUDE.md (local project first, then global):
1. `playbooks/tasks/**/plan-feature.md` in the current project
2. `~/.claude/playbooks/tasks/**/plan-feature.md`

First hit wins. Read the resolved file in full and execute it, treating everything below as its input. If neither path resolves, say so instead of guessing.

---
$ARGUMENTS
